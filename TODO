use header and readme in indexes
add nightlydev tools
add friendly urls tool
copy key to clipboard
show import key command
clean rubbish Peer class
show count of results in searches
show render time as comment
add gettext
add help pages
write code documentation
goto sleep
